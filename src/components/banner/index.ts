import PageBanner from "./PageBanner"

type BannerPropType = {
    header: string;
    subheader?: string;
    image: string;
    height?: string;
}

export type {
    BannerPropType,
}

export {
    PageBanner,
}